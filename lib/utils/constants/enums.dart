// LIST OF Enums
// They cannot be created inside a class.

enum TextSizes { small, medium, large }

enum OrderStatus {
  pending,
  confirmed,
  processing,
  shipped,
  outForDelivery,
  delivered,
  cancelled,
  returned
}

enum PaymentMethods {
  upi,
  paytm,
  phonePe,
  googlePay,
  razorPay,
  creditCard,
  debitCard,
  cod
}